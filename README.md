# Homebrew Tap for MiniNote

Install MiniNote with Homebrew:

```bash
brew tap MoeMoeGit/mininote
brew install --cask mininote
```

Upgrade MiniNote:

```bash
brew upgrade --cask mininote
```

MiniNote releases are published from:

https://github.com/vivalucas/mininote/releases
